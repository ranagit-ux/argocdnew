apiVersion: apps/v1
kind: Deployment
metadata:
  name: uat-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: uat-frontend
  template:
    metadata:
      labels:
        app: uat-frontend
    spec:
      containers:
        - name: frontend
          image: nginx:1.27
          ports:
            - containerPort: 80
